name: Daily MLB Pipeline

# Runs every morning at 6:00 ET (10:00 UTC), again at 1:00 ET (line refresh),
# and again at 6:00 PM ET. Pulls live odds + schedule, runs the model, commits
# the refreshed data/*.json back to main. The site reads what's in main.

on:
  schedule:
    # 2026-05-21: dropped 1 PM ET refresh to fit GH Actions quota.
    # Each run is ~9.5 min and we are at 90% of 2,000 included minutes.
    # 3x/day -> 2x/day saves ~9.5 min/day. Re-enable 1 PM cron after June 1 reset.
    - cron: '0 10 * * *'   # 6 AM ET — full slate setup
    - cron: '0 22 * * *'   # 6 PM ET — final pre-game refresh
  workflow_dispatch:        # manual trigger button (use for line-shop refresh)

# Needed for git push (default GITHUB_TOKEN is read-only otherwise).
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: pip install -r python/requirements.txt

      # Smoke tests: every module imports + critical data shapes valid +
      # Bovada reachable + calibration shrinkage works. Fast (<10s).
      # Fails the pipeline early on obvious breakage rather than letting
      # garbage propagate to today.json.
      - name: Smoke tests
        working-directory: python
        run: python smoke_tests.py

      # Snapshot the active config (defaults + any runtime overrides) so
      # /config.html can render the live knob table.
      - name: Snapshot config
        working-directory: python
        run: python config.py dump

      # Diff data/runtime_config.json against the last seen snapshot. Any
      # change since prior cron (override added / removed / changed) is
      # appended to data/config_history.json for the audit trail.
      - name: Log config override changes
        working-directory: python
        run: python config_history.py

      # Settle yesterday's projections BEFORE generating today's, so today's
      # projections incorporate the latest calibration corrections.
      - name: Settle yesterday's outcomes
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python outcomes.py

      - name: Recompute calibration corrections
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python calibration_runner.py

      # Where-is-the-model-wrong analysis: per-market residuals, histograms,
      # per-player systematic patterns. Surfaces /residuals.html.
      - name: Compute residual analysis
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python residuals.py

      # Performance-over-time trend: per-day hit rate, Brier, MAE, ROI.
      # Lets us see if the model is getting better as calibration accumulates.
      - name: Compute performance trend
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python perf_trend.py

      # Anomaly detection: flag player/market pairs the model is structurally
      # wrong on (bias correction can't fix systematic player-level errors).
      - name: Detect player + day anomalies
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python anomalies.py

      # Single-number model confidence score (0-100) + tier label.
      # Aggregates calibration / sample / residual / ROI signals.
      - name: Compute model confidence score
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python model_confidence.py

      # Per-player gamelogs (last 14 games/starts) for the sparkline on
      # the player deep-dive page. Caps at 350 players to respect API quota.
      # NOW enriched with venue + is_home + is_night per game so downstream
      # park / day-night / travel splits work without extra API calls.
      - name: Pull player gamelogs (last 14)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python player_gamelogs.py

      # One-shot enrichment of track_record settled props with HP umpire +
      # venue + day/night tags (where missing). Caps at 500 records per run
      # to be polite to MLB Stats API. Idempotent + safe to repeat.
      - name: Backfill track_record context (ump / venue / night)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python backfill_context.py

      # Per-player deep-dive precompute: aggregates season + career + L/R + H/A
      # splits from MLB Stats API (free), joins with matchups for BvP H2H +
      # pitch-arsenal xwOBA, derives park / day-night / travel / vs-umpire
      # from the enriched gamelog + track_record. Powers /player.html.
      # MUST RUN BEFORE rationales.py so the rationales can fold breakdown
      # signals (hot/cold, park splits, BvP) into each prop's drivers.
      - name: Build player breakdowns (splits + BvP + park + travel + umpire)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python player_breakdown.py

      # Per-prop rationale: "why this play" with model factors, season stats,
      # Statcast metrics, calibration disclaimers + breakdown-enriched
      # drivers (hot/cold, vs-handedness, park, BvP H2H, lineup spot, etc.).
      # Caps at top 300 by edge.
      - name: Generate prop rationales (breakdown-enriched)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python rationales.py

      # Drift detection: compare today's calibration vs yesterday's.
      # Alerts on >5% correction moves or sudden sample-count drops.
      - name: Detect calibration drift
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python drift_detector.py

      # Walk-forward backtest: rolling-window evaluation proving the model
      # IS improving (Brier shrinks day over day with bias correction applied).
      - name: Walk-forward backtest
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python walk_forward.py

      # Per-player bias overrides: bespoke multipliers for players the
      # market-level bias correction can't fix (z >= 2 systematic outliers).
      - name: Compute per-player bias overrides
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python player_bias.py

      # Secondary multiplier tuner: grid-search amplifier on opp_mult,
      # umpire_k_mult, park_hand_mult per market.
      - name: Tune secondary multipliers
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python multiplier_tuner.py

      # Training feedback log: writes a "what changed this run" entry to
      # training_feedback.json so /training renders a feed of recent activity.
      - name: Log training feedback
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python training_feedback.py

      # Parameter recommender: analyzes calibration / walk-forward / track-record
      # and writes data/param_recommendations.json with suggested config knob
      # changes. /config.html renders these with one-click Apply links to
      # runtime_config.json on GitHub. Suggestions only -- never auto-applied.
      - name: Generate parameter recommendations
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python param_recommender.py

      # Override effect tracker: for each entry in config_history.json,
      # compute pre/post 7-day Brier + hit rate from track_record. Lets the
      # operator (and the recommender) see whether each tweak actually
      # improved the model. Surfaces on /config.
      - name: Measure override effects
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python override_effects.py

      # Pipeline health audit: empty/demo/stale-artifact detection.
      # Runs LAST so it captures the final state of every artifact.
      # Surfaces /audit.html.

      - name: Run daily pipeline (real data → today.json + matchups.json + snapshot)
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        working-directory: python
        run: python pipeline.py

      # Player-props refresh. EDGESTAT_PROPS_MAX_GAMES controls the quota cap:
      # set to 16 on the $20 tier (20k req/month), 8 on free tier (500/month).
      - name: Run player-props pipeline (DraftKings)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
          EDGESTAT_PROPS_MAX_GAMES: ${{ vars.EDGESTAT_PROPS_MAX_GAMES || '16' }}
        working-directory: python
        run: python props_pipeline.py

      # Unify today.json (model line + market + recs) with every per-matchup prop
      # into one card per game -> data/mlb_game_cards.json, powering the clickable
      # per-game detail view (game.html). Runs after props so it can join them.
      - name: Build per-game cards (game.html detail view)
        working-directory: python
        run: python mlb_game_cards.py

      # League-wide Trends hub: ATS under/over-rates, hot/cold teams + bats,
      # pitcher form leaderboard, run-environment -> data/trends_hub.json (trends.html).
      - name: Build league-wide Trends hub
        working-directory: python
        run: python trends_hub.py

      # High-Confidence Board + props-only parlays (read the joined cards/props).
      - name: Build high-confidence board
        working-directory: python
        run: python high_confidence_board.py

      - name: Build props-only parlays
        working-directory: python
        run: python props_parlay_builder.py

      # Regression guard: prove the board's two real-outcome guards are live --
      # curation drops market families proven to lose money, empirical calibration
      # blends probs toward realized hit rates, and zero proven-negative plays
      # shipped. Fails the build if a guard regresses.
      - name: Verify calibration guards
        working-directory: python
        run: python verify_calibration.py

      # Publish the model calibration map (predicted vs realized per market family,
      # the curation + calibration evidence) -> data/calibration_map.json ->
      # calibration-map.html. Show-your-work transparency from the settled ledger.
      - name: Build model calibration map
        working-directory: python
        run: python calibration_map.py

      # Real overall reliability diagram (Brier/ECE/buckets) from settled outcomes,
      # overwriting the old synthetic demo -> data/calibration.json (ML Lab chart).
      - name: Build real calibration report
        working-directory: python
        run: python calibration_real.py

      # Build parlay suggestions from today's qualifying single-leg edges.
      - name: Run parlay engine
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python parlays.py

      # Correlation-aware Same-Game Parlay (SGP) builder. For each game,
      # finds top-EV 2-4 leg combos using a pairwise correlation matrix
      # (e.g. same-player HR + TB are tightly coupled). Books charge ~25%
      # extra on SGPs assuming legs are independent -- this surfaces the
      # gap. Output: data/sgps.json -> /parlays.html top card.
      - name: Build correlation-aware Same-Game Parlays
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python sgp_builder.py

      # Pitcher fatigue + early-hook risk grade. Powers /mlb panel.
      - name: Grade pitcher fatigue + early-hook risk
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python pitcher_fatigue.py

      # Weather-conditional HR-rate delta + OVER/UNDER lean per game.
      - name: Score weather-conditional HR multiplier
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python weather_conditional.py

      # Pre-game slate confidence (lineup confirms / weather / rookies / hooks).
      # MUST run after pitcher_fatigue + weather_conditional so it sees them.
      - name: Compute pre-game slate confidence
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python slate_confidence.py

      # Lineup churn watcher: diff matchups.json vs prior snapshot.
      # Runs in the live workflow too so churn surfaces between cron cycles.
      - name: Detect lineup churn (post-publish scratches)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python lineup_churn.py

      # Composite Best Bets ranker: rolls up DK + PP + NRFI + SGPs into
      # a single quality-scored list. Runs LAST so it sees the latest
      # of every upstream artifact. Powers /best-bets.html.
      - name: Compose Best Bets digest (composite ranker)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python best_bets.py

      # PrizePicks Value Board: scans the PP slate for legs whose CALIBRATED hit
      # rate clears PrizePicks' flat-payout break-even. Overconfident families
      # dropped, uncalibrated props excluded -> data/prizepicks_value.json.
      - name: Build PrizePicks value board
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python prizepicks_value.py

      # Bullpen exhaustion -> late-inning OU lean per game.
      - name: Score bullpen workload alerts
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python bullpen_alert.py

      # Risk-adjusted Sharpe ranking of tonight's plays.
      - name: Rank by Sharpe (EV / sigma)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python sharpe_ranking.py

      # Auto-portfolio: optimal multi-bet subset given bankroll + Kelly +
      # slate-confidence-adjusted quality gate + per-game correlation cap.
      - name: Build auto-portfolio
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python portfolio_optimizer.py

      # 10K MC simulator of tonight's portfolio outcomes. Powers /bankroll
      # "Tonight's Portfolio" card with real-money distribution.
      - name: Monte Carlo tonight's portfolio
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python bankroll_simulator.py

      # Line movement / bet-timing: ACT NOW vs WAIT alerts from price
      # diffs cycle-over-cycle. Surfaces on /props.
      - name: Detect line-movement timing signals
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python line_timing.py

      # Per-bet CLV tracker -- proves bets beat the close (sharpness proxy).
      - name: Compute per-bet CLV
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python clv_per_bet.py

      # Day-of-week P&L analysis from settled track_record.
      - name: Day-of-week performance analysis
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python dow_pl.py

      # Hot-streak heatmap for tonight's slate.
      - name: Score hot streaks (cross-slate heatmap)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python hot_streaks.py

      # Cross-book arb / shading detector (DK vs PrizePicks).
      - name: Detect cross-book arb opportunities
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python cross_book_arb.py

      # Pitcher fastball velocity decay flag (injury proxy).
      - name: Flag pitcher velocity decay
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python velocity_decay.py

      # Daily picks CSV export.
      - name: Export picks CSV
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python csv_export.py

      # NBA + NFL stubs (will start producing real data when enabled in config)
      - name: NBA stub artifact (back-compat)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python nba_stub.py

      # === DEEP DATA LAYER FIRST (so sport pipelines pick up fresh calibration shifts) ===
      # Self-training writes self_training_<sport>.json with bias corrections;
      # nba/nhl/wnba/etc pipelines read it via _self_training_shift() and apply
      # the shift to p_home_win. So this must run BEFORE the sport pipelines.
      - name: Pull last-14-days historical games per sport
        working-directory: python
        run: python historical_games.py
      - name: Compute per-team form (L5/L10) per sport
        working-directory: python
        run: python multi_sport_team_form.py
      - name: Compute head-to-head history per sport
        working-directory: python
        run: python head_to_head.py
      - name: Self-training backtest (per-sport bias + calibration shift)
        working-directory: python
        run: python self_train.py

      # NOW run sport pipelines so they apply the freshly-computed calibration shifts
      - name: NBA live scoreboard + win probabilities (post-self-train)
        working-directory: python
        run: python nba_pipeline.py
      - name: NHL live scoreboard + win probabilities (post-self-train)
        working-directory: python
        run: python nhl_pipeline.py
      - name: Pull ESPN team rosters (NBA/WNBA/NHL/NFL) -- 4,618 players
        working-directory: python
        run: python espn_rosters.py
      - name: Pull per-player game logs (last 20 games) for NBA/WNBA/NHL starters
        working-directory: python
        run: python espn_player_stats.py
      - name: Generate REAL player prop projections from per-game averages
        working-directory: python
        run: python real_player_props.py
      - name: Per-team residual analysis (over/underrated)
        working-directory: python
        run: python team_residuals.py
      - name: Player trends L5 vs L20 (heating up / cooling down)
        working-directory: python
        run: python player_trends.py
      - name: Multi-sport portfolio builder (quality-weighted)
        working-directory: python
        run: python multi_sport_portfolio.py
      - name: Drift detector v2 (prob shifts + model degradation)
        working-directory: python
        run: python drift_detector_v2.py
      - name: Cross-sport CLV tracker (morning snapshot + intraday compare)
        working-directory: python
        run: python clv_tracker_multi.py
      - name: Heat/cold alert aggregator (hot teams + trending players)
        working-directory: python
        run: python heat_cold_alerts.py
      - name: Pre-game alert system (form-aligned high conviction picks)
        working-directory: python
        run: python pregame_alerts.py
      - name: MLB batter per-game logs (real prop projections for 372+ hitters)
        working-directory: python
        run: python mlb_batter_logs.py
      - name: MLB batter heat tracker (L14 AVG vs season delta)
        working-directory: python
        run: python mlb_batter_heat.py
      - name: MLB extended player props (TB / BB / SO / HRR / outs / extended K)
        working-directory: python
        run: python mlb_extended_props.py
      - name: MLB firehose props (2+ hits / 3+ TB / 1+ RBI / alt game totals)
        working-directory: python
        run: |
          python mlb_batter_2plus_hits_props.py || true
          python mlb_batter_3plus_tb_props.py || true
          python mlb_batter_rbi_props.py || true
          python mlb_game_total_alt_props.py || true
      - name: NBA player heat tracker (L5 vs season PTS/REB/AST deltas)
        working-directory: python
        run: python nba_player_heat.py
      - name: NBA extended props (DD/TD/PR/PA/RA combos + alt-3PM)
        working-directory: python
        run: python nba_extended_props.py
      - name: NHL extended props (ATG/goals/assists/points/SOG, playoff-filtered)
        working-directory: python
        run: python nhl_extended_props.py
      - name: NHL team props (total goals / BTTS / first period / per-team)
        working-directory: python
        run: python nhl_team_props.py
      - name: Soccer extended team props (EPL/MLS/UCL goals/BTTS/clean-sheet)
        working-directory: python
        run: python soccer_extended_props.py
      - name: Slate-wide player prop aggregator (5K+ sweet-spot picks)
        working-directory: python
        run: python slate_player_pot.py
      - name: Slate-wide TEAM market aggregator (ML/totals/BTTS/specials)
        working-directory: python
        run: python slate_team_pot.py
      - name: Todays Top Plays curated board (>=70% prob, Kelly-ranked)
        working-directory: python
        run: python todays_top_plays.py
      - name: WNBA player heat tracker (L5 vs season -- tight thresholds)
        working-directory: python
        run: python wnba_player_heat.py
      - name: UFC fight matchup engine (career-record ELO + experience)
        working-directory: python
        run: python ufc_matchup.py
      - name: UFC method-of-victory (KO / Sub / Decision + fight-doesn't-go-distance)
        working-directory: python
        run: python ufc_method_of_victory.py
      - name: Golf player heat (recent finish + Sunday-round momentum)
        working-directory: python
        run: python golf_player_heat.py
      - name: Golf region props (Top USA / Europe / Asia / etc.)
        working-directory: python
        run: python golf_region_props.py
      - name: ATS tracker (model cover% per team)
        working-directory: python
        run: python ats_tracker.py
      - name: Multi-sport correlation / regime detector
        working-directory: python
        run: python multi_sport_correlation.py
      - name: Daily prediction log (append + auto-settle from historicals)
        working-directory: python
        run: python daily_prediction_log.py
      - name: NFL survivor pool helper (in-season picks ranked by tier)
        working-directory: python
        run: python survivor_pool.py
      - name: Daily summary v2 markdown digest (all signals + portfolio + self-training)
        working-directory: python
        run: python daily_summary_v2.py
      - name: MLB starting pitcher game logs (real per-start stats from MLB API)
        working-directory: python
        run: python mlb_pitcher_logs.py
      - name: MLB pitcher matchup engine (K/ER prop projections per game)
        working-directory: python
        run: python mlb_pitcher_matchup.py
      - name: MLB pitcher DEEP matchup (TTO + opp lineup K-rate + form + park stack)
        working-directory: python
        run: python mlb_pitcher_deep_matchup.py
      - name: MLB catcher framing K-add overlay (pitcher prop K-shading)
        working-directory: python
        run: python mlb_catcher_framing.py
      - name: MLB batter-vs-SP matchup edges (xwOBA arsenal + career split adjustments)
        working-directory: python
        run: python mlb_batter_sp_edges.py
      - name: MLB pitcher form regression (recent vs season blend + streak flags)
        working-directory: python
        run: python mlb_pitcher_form_regression.py
      - name: MLB team-total edges (decompose model fair_total -> per-team Poisson lines)
        working-directory: python
        run: python mlb_team_total_edges.py
      - name: MLB run-line edges (Poisson convolution on per-team lambdas)
        working-directory: python
        run: python mlb_run_line_edges.py
      - name: "MLB under alerts (multi-signal under detector: park + weather + pitchers + ump)"
        working-directory: python
        run: python mlb_unders_alerts.py
      - name: MLB starter pitch count + IP projection
        working-directory: python
        run: python mlb_starter_pitch_count.py
      - name: MLB lineup strength + mismatch detector
        working-directory: python
        run: python mlb_lineup_strength.py
      - name: MLB bullpen freshness tracker (compound late-inning alerts)
        working-directory: python
        run: python mlb_bullpen_freshness.py
      - name: NHL playoff goalie matchup (blended SV%, expected goals, team totals + shutout)
        working-directory: python
        run: python nhl_goalie_matchup.py
      - name: NHL skater props (anytime goal, SOG, points, multi-point markets)
        working-directory: python
        run: python nhl_skater_props.py
      - name: RE-RUN slate aggregators (fold in all new sport-specific modules)
        working-directory: python
        run: |
          python slate_player_pot.py
          python slate_team_pot.py
          python todays_top_plays.py
      - name: Play of the Day P&L tracker (PRIMARY accuracy benchmark, 1u flat)
        working-directory: python
        run: python pod_pl_tracker.py
      - name: POD factor decomposition (real per-factor contributions, not seed mock)
        working-directory: python
        run: python pod_factor_decomp.py
      - name: Locks of the Day (record top 5 picks + settle any pending)
        working-directory: python
        run: python locks_of_day.py
      # Real box-score outcomes (player gamelogs + finals) so non-MLB picks settle
      # + feed the learning loop instead of voiding. Must run BEFORE the all-picks
      # ledger so the fresh logs are present at settlement. WNBA is live now; NBA
      # and NFL return 0 games off-season and auto-activate the moment their
      # seasons start (Oct / Sept) -- the graders are already wired.
      - name: Box-score logs for settlement (WNBA live; NBA + NFL season-ready)
        working-directory: python
        run: |
          python espn_box_logs.py wnba || true
          python espn_box_logs.py nba || true
          python espn_box_logs.py nfl || true
          python espn_box_logs.py nhl || true
          python espn_event_results.py golf || true
          python espn_event_results.py tennis || true
          python espn_event_results.py ufc || true
          python espn_event_results.py f1 || true
          python espn_event_results.py soccer || true
      - name: All-picks unified ledger (every surfaced pick -> training signal)
        working-directory: python
        run: python all_picks_tracker.py
      - name: Self-learn shrinkage weights (per-source feedback loop)
        working-directory: python
        run: python self_learn_weights.py

      # Generator-level recalibration: learn an isotonic prob->realized transform per
      # market family from the freshly-settled ledger (ECE ~11% -> ~3%). Makes the
      # model's probabilities honest at the source. -> data/recalibration_map.json
      - name: Generator-level recalibration (isotonic, from settled outcomes)
        working-directory: python
        run: python recalibration.py
      - name: Live projection trainer (stat-scale RMSE/bias from the free feeds)
        working-directory: python
        run: python projection_trainer.py || true
      - name: Strategy simulator replay deck (settled ledger -> strategy-sim.html)
        working-directory: python
        run: python strategy_sim.py
      - name: Cross-sport parlay builder (2-leg + 3-leg combos with same-game correlation)
        working-directory: python
        run: python cross_sport_parlay_builder.py
      - name: Weekly recap (last-7-day locks performance summary)
        working-directory: python
        run: python weekly_recap.py
      - name: Calibration status snapshot (per-source shrinkage transparency)
        working-directory: python
        run: python calibration_status.py
      - name: Tonight's brief (60-second action-only markdown summary)
        working-directory: python
        run: python tonight_brief.py
      - name: Locks CLV tracker (closing line value per lock -- early ROI signal)
        working-directory: python
        run: python locks_clv.py
      - name: Sharp action radar (top-25 pick line movement)
        working-directory: python
        run: python sharp_action_radar.py
      - name: Reverse Line Movement detector (sharp money signal)
        working-directory: python
        run: python reverse_line_movement.py || true
      - name: MLB Total Bases props (1+/2+/3+ TB Poisson projections)
        working-directory: python
        run: python mlb_total_bases_props.py || true
      - name: MLB HRR (Hits+Runs+RBI) combo prop projections
        working-directory: python
        run: python mlb_hrr_props.py || true
      - name: MLB Doubles / Extra-Base Hits prop projections
        working-directory: python
        run: python mlb_doubles_props.py || true
      - name: MLB Pitcher Walks (BB) prop projections
        working-directory: python
        run: python mlb_pitcher_walks_props.py || true
      - name: MLB Pitcher Outs Recorded prop projections
        working-directory: python
        run: python mlb_pitcher_outs_props.py || true
      - name: NBA player points props (per-player projections)
        working-directory: python
        run: python nba_player_points_props.py || true
      - name: NBA player assists props
        working-directory: python
        run: python nba_player_assists_props.py || true
      - name: NBA player rebounds props
        working-directory: python
        run: python nba_player_rebounds_props.py || true
      - name: NHL skater hits + blocks + PIM props
        working-directory: python
        run: python nhl_skater_hits_blocks_props.py || true
      - name: NHL goalie saves prop projections
        working-directory: python
        run: python nhl_goalie_saves_props.py || true
      - name: NHL per-period totals (1st/2nd/3rd period over/under)
        working-directory: python
        run: python nhl_period_totals_props.py || true
      - name: NHL anytime goalscorer prop projections
        working-directory: python
        run: python nhl_anytime_goal_props.py || true
      - name: NHL skater shots-on-goal (SOG) prop projections
        working-directory: python
        run: python nhl_skater_sog_props.py || true
      - name: NHL skater points (G+A) prop projections
        working-directory: python
        run: python nhl_skater_points_props.py || true
      - name: NBA player PRA combo prop projections
        working-directory: python
        run: python nba_player_pra_props.py || true
      - name: NBA player free-throw attempts (FTA) prop projections
        working-directory: python
        run: python nba_player_ft_attempts_props.py || true
      - name: Soccer total shots prop projections
        working-directory: python
        run: python soccer_total_shots_props.py || true
      - name: NBA player 3-pointers (3PM) props
        working-directory: python
        run: python nba_player_threes_props.py || true
      - name: NBA team alt-totals + 1H/1Q totals + alt-spreads
        working-directory: python
        run: python nba_team_alts_props.py || true
      - name: NBA player blocks + steals prop projections
        working-directory: python
        run: python nba_player_blocks_steals_props.py || true
      - name: NBA double-double yes/no prop projections
        working-directory: python
        run: python nba_double_double_props.py || true
      - name: NBA player turnovers prop projections
        working-directory: python
        run: python nba_player_turnovers_props.py || true
      # Combo markets (derive from the points/assists/rebounds prop outputs above).
      - name: NBA player combo props (PA / PR / RA)
        working-directory: python
        run: |
          python nba_player_pa_combo_props.py || true
          python nba_player_pr_combo_props.py || true
          python nba_player_ra_combo_props.py || true
      - name: WNBA player 3-pointers (3PM) props
        working-directory: python
        run: python wnba_player_threes_props.py || true
      - name: WNBA player blocks+steals combo prop
        working-directory: python
        run: python wnba_player_blocks_steals_props.py || true
      - name: MLB pitcher rest-day advantage model
        working-directory: python
        run: python mlb_pitcher_rest_advantage.py || true
      - name: NBA team total points (O/U) prop projections
        working-directory: python
        run: python nba_team_total_props.py || true
      - name: NHL goalie shutout yes/no prop projections
        working-directory: python
        run: python nhl_goalie_shutout_props.py || true
      - name: NHL first goalscorer prop projections
        working-directory: python
        run: python nhl_first_goalscorer_props.py || true
      - name: Tennis match-win (ELO) prop projections
        working-directory: python
        run: python tennis_match_win_props.py || true
      - name: Tennis total games prop projections
        working-directory: python
        run: python tennis_total_games_props.py || true
      - name: Tennis set-score prop projections
        working-directory: python
        run: python tennis_set_score_props.py || true
      - name: MLB batter walks (1+) yes/no prop
        working-directory: python
        run: python mlb_batter_walks_props.py || true
      - name: MLB batter strikeout (1+/2+) yes/no prop
        working-directory: python
        run: python mlb_batter_strikeout_props.py || true
      - name: MLB extra-innings yes/no game prop
        working-directory: python
        run: python mlb_extra_innings_prop.py || true
      - name: NHL goalie wins yes/no prop
        working-directory: python
        run: python nhl_goalie_win_props.py || true
      - name: Tennis player aces O/U prop
        working-directory: python
        run: python tennis_aces_props.py || true
      # 2026-05-21: batched runner for all prop modules. Safety net that re-runs
      # the ~62 pure-projection modules in a SINGLE Python process to share
      # startup overhead. Next cycle, the individual steps above can be removed.
      - name: Batched prop-module runner (saves Python startup overhead)
        working-directory: python
        run: python run_props_batch.py || true
      # Runs AFTER run_props_batch + bullpen_freshness so the situational rollup
      # (steam / compound bullpen / pen mismatch / ump extremes) reads THIS run's
      # fresh feeds, not the previous run's.
      - name: Cross-sport anomaly detector (streak / residual / situational rollup)
        working-directory: python
        run: python anomaly_detector.py || true
      - name: WNBA player rebounds + assists props
        working-directory: python
        run: python wnba_player_reb_ast_props.py || true
      - name: WNBA team alt-totals + 1H/1Q + alt-spreads
        working-directory: python
        run: python wnba_team_alts_props.py || true
      - name: Soccer total corners props (MLS/EPL/UCL)
        working-directory: python
        run: python soccer_corners_props.py || true
      - name: MLB Pitcher Strikeouts (K) prop projections
        working-directory: python
        run: python mlb_pitcher_strikeouts_props.py || true
      - name: MLB Pitcher Earned Runs (ER) prop projections
        working-directory: python
        run: python mlb_pitcher_er_props.py || true
      - name: MLB Pitcher Hits Allowed prop projections
        working-directory: python
        run: python mlb_pitcher_hits_allowed_props.py || true
      - name: MLB first batter retired prop projections
        working-directory: python
        run: python mlb_first_batter_retired_props.py || true
      - name: MLB team stolen base total prop projections
        working-directory: python
        run: python mlb_team_sb_total_props.py || true
      - name: WNBA player points props
        working-directory: python
        run: python wnba_player_pts_props.py || true
      - name: Golf top-N finish props (top-5/10/20/40)
        working-directory: python
        run: python golf_top_finish_props.py || true
      - name: Soccer BTTS (Both Teams To Score) prop projections
        working-directory: python
        run: python soccer_btts_props.py || true
      - name: Soccer total goals over/under prop projections
        working-directory: python
        run: python soccer_total_goals_props.py || true
      - name: Soccer clean sheet prop projections
        working-directory: python
        run: python soccer_clean_sheet_props.py || true
      - name: Soccer anytime goalscorer props
        working-directory: python
        run: python soccer_goalscorer_props.py || true
      - name: Soccer total cards (yellow + red) prop projections
        working-directory: python
        run: python soccer_cards_props.py || true
      - name: MLB First-5-Innings market (F5 ML + F5 totals, sharp-favored)
        working-directory: python
        run: python mlb_first5_market.py || true
      - name: MLB innings 1-3 total prop projections
        working-directory: python
        run: python mlb_innings_1_3_totals.py || true
      - name: MLB innings 4-6 total prop projections (bullpen transition)
        working-directory: python
        run: python mlb_innings_4_6_totals.py || true
      - name: MLB innings 7-9 total prop projections (closer + setup)
        working-directory: python
        run: python mlb_innings_7_9_totals.py || true
      - name: MLB first inning HR yes/no prop projections
        working-directory: python
        run: python mlb_first_inning_hr_yn.py || true
      - name: MLB to-record-a-hit yes/no per batter
        working-directory: python
        run: python mlb_to_record_hit_yn.py || true
      - name: MLB pitcher Quality Start (6 IP, ≤3 ER) yes/no prop
        working-directory: python
        run: python mlb_pitcher_quality_start_props.py || true
      - name: MLB to-score-a-run yes/no per batter
        working-directory: python
        run: python mlb_to_score_run_yn.py || true
      - name: MLB Stolen Base props (delivery x pop time x runner speed)
        working-directory: python
        run: python mlb_steal_props.py || true
      - name: NBA pace-adjusted total projections
        working-directory: python
        run: python nba_pace_adjusted.py || true
      - name: MLB starter quality composite (0-100 ranking)
        working-directory: python
        run: python mlb_starter_quality_composite.py || true
      - name: Multi-book line aggregator (Bovada + ESPN + Odds API)
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        working-directory: python
        run: python multi_book_lines.py || true
      - name: "Whale picks (max-confluence: top board + consensus + sharp action)"
        working-directory: python
        run: python whale_picks.py
      - name: Pick evolution tracker (stable vs flickering picks across cycles)
        working-directory: python
        run: python pick_evolution_tracker.py
      - name: MLB batter situational splits (home/away + day/night log-odds shifts)
        working-directory: python
        run: python mlb_batter_situational_splits.py
      - name: MLB batter L/R pitcher-handedness splits (season vsL/vsR vs opposing SP hand)
        working-directory: python
        run: python mlb_batter_lvr_splits.py
      - name: MLB pitcher LvR x opposing-lineup handedness (weighted K/9 projection)
        working-directory: python
        run: python mlb_pitcher_lvr_lineup.py
      - name: MLB batter ADVANCED splits (RISP + late-close + day-night + home-away)
        working-directory: python
        run: python mlb_batter_advanced_splits.py || true
      - name: Consensus picks (>=2 independent modules agree on same batter)
        working-directory: python
        run: python consensus_picks.py
      - name: Golf LIVE projections (per-player p_top_5/10/cut from current state)
        working-directory: python
        run: python golf_live_projections.py
      - name: Golf LIVE tracker (heaters, stalled, cut-bubble, position-movers)
        working-directory: python
        run: python golf_live_tracker.py
      - name: Cross-signal convergence detector (everything-aligned plays)
        working-directory: python
        run: python convergence_detector.py
      - name: NBA / NHL back-to-back fatigue tracker
        working-directory: python
        run: python b2b_fatigue.py
      - name: Streak regression-to-mean detector (W3+/L3+)
        working-directory: python
        run: python streak_regression.py
      - name: Refresh legacy ML stack (train.py - 14 artifacts in ~10s)
        working-directory: python
        run: python train.py || true
      - name: Refresh legacy arbitrage / linemaker / bankroll-MC / kelly-heatmap
        working-directory: python
        run: |
          python arbitrage.py || true
          python linemaker.py || true
          python risk.py || true
      - name: Live data accuracy audit (cross-check upstream APIs)
        working-directory: python
        run: python data_health.py || true
      - name: Master audit (imports + HTML + data + health)
        working-directory: python
        run: python _audit_master.py || true
      - name: Training convergence tracker (per-market learning trajectory)
        working-directory: python
        run: python training_convergence.py || true
      - name: Model health score (one-number summary)
        working-directory: python
        run: python model_health.py || true
      - name: Data health check (live source cross-verification)
        working-directory: python
        run: python data_health.py

      # === Extended ESPN sport coverage (10 new sports) ===
      - name: WNBA live scoreboard
        working-directory: python
        run: python wnba_pipeline.py
      - name: WNBA extended props (DD/TD/combos/alt-3PM -- uses fixed WNBA stat indices)
        working-directory: python
        run: python wnba_extended_props.py
      - name: MLS soccer scoreboard
        working-directory: python
        run: python mls_pipeline.py
      - name: EPL soccer scoreboard
        working-directory: python
        run: python epl_pipeline.py
      - name: UEFA Champions League scoreboard
        working-directory: python
        run: python ucl_pipeline.py
      - name: 2026 World Cup scoreboard (June 11 - July 19)
        working-directory: python
        run: python worldcup_pipeline.py || true
      - name: NFL live scoreboard
        working-directory: python
        run: python nfl_pipeline.py
      # NFL player-prop projections (pass/rush/rec yds, receptions, anytime TD).
      # Reads the fresh nfl_state slate -> model-conviction picks collected by the
      # next ledger cycle + settled vs box scores. 0 off-season; live Week 1.
      - name: NFL player-prop projections (model side -> generate->settle->learn)
        working-directory: python
        run: python nfl_player_props.py || true
      - name: NCAA Football scoreboard
        working-directory: python
        run: python ncaaf_pipeline.py
      - name: NCAA Men's Basketball scoreboard
        working-directory: python
        run: python ncaab_pipeline.py
      - name: NCAA Softball / Women's CWS scoreboard
        working-directory: python
        run: python ncaa_softball_pipeline.py
      - name: NCAA Baseball / CWS scoreboard
        working-directory: python
        run: python cws_pipeline.py
      - name: F1 race scoreboard
        working-directory: python
        run: python f1_pipeline.py
      - name: F1 driver projections (skill-z based win/podium/points for upcoming race)
        working-directory: python
        run: python f1_driver_projections.py
      - name: F1 track-specific history (per-driver last-5 finish at this circuit)
        working-directory: python
        run: python f1_track_specific.py || true
      - name: F1 qualifying predictor (pole / top-3 / top-6)
        working-directory: python
        run: python f1_qualifying_predictor.py || true
      - name: UFC significant strikes + takedowns prop projections
        working-directory: python
        run: python ufc_strikes_takedowns_props.py || true
      - name: UFC first-round-finish props (settle vs the fight's ending round)
        working-directory: python
        run: python ufc_first_round_finish.py || true
      - name: UFC fight card scoreboard
        working-directory: python
        run: python ufc_pipeline.py

      # === KBO (Korean Baseball Organization) full stack ===
      - name: KBO live schedule pull
        working-directory: python
        run: python kbo_pipeline.py
      - name: KBO starting pitcher matchup adjustments
        working-directory: python
        run: python kbo_pitcher_adj.py
      - name: KBO Poisson MC win-prob + total model
        working-directory: python
        run: python kbo_model.py
      - name: KBO Play of the Day selector
        working-directory: python
        run: python kbo_bestbet.py
      - name: KBO POD rolling history
        working-directory: python
        run: python kbo_pot_history.py
      - name: KBO player roster + per-game projections + prop fair odds
        working-directory: python
        run: python kbo_players.py

      # === Cross-sport Player POT (scans all 3 sport prop boards) ===
      - name: Player Play of the Day across LoL + CS + KBO
        working-directory: python
        run: python player_pot.py
      - name: Player POT rolling history (with auto-settle)
        working-directory: python
        run: python player_pot_history.py

      # === Advanced features: DFS optimizer + picks export ===
      - name: DFS Power-Play lineup optimizer
        working-directory: python
        run: python dfs_optimizer.py
      - name: Picks CSV/JSON export
        working-directory: python
        run: python picks_export.py

      # === League of Legends esports full stack ===
      - name: LoL Riot esports schedule pull
        working-directory: python
        run: python lol_pipeline.py
      - name: LoL ELO + series win-prob model
        working-directory: python
        run: python lol_model.py
      - name: LoL Play of the Tournament selector
        working-directory: python
        run: python lol_bestbet.py
      - name: LoL POT rolling history
        working-directory: python
        run: python lol_pot_history.py
      - name: LoL 2-3 leg parlay builder
        working-directory: python
        run: python lol_parlay.py
      - name: LoL player roster pull (982 players from Riot API)
        working-directory: python
        run: python lol_players.py
      - name: LoL per-player projections + prop fair odds
        working-directory: python
        run: python lol_player_proj.py

      # === Counter-Strike (CS2) full stack ===
      - name: CS Liquipedia + HLTV top-30 pull
        working-directory: python
        run: python cs_pipeline.py
      - name: CS ELO + series win-prob model
        working-directory: python
        run: python cs_model.py
      - name: CS Play of the Day selector
        working-directory: python
        run: python cs_bestbet.py
      - name: CS POD rolling history
        working-directory: python
        run: python cs_pot_history.py
      - name: CS map handicap props (sweep / distance / total)
        working-directory: python
        run: python cs_maphandicap.py
      - name: CS player roster + per-BO3 projections + prop fair odds
        working-directory: python
        run: python cs_players.py

      # === Self-learning calibration across LoL + CS + KBO ===
      - name: Esports + KBO reliability calibration
        working-directory: python
        run: python esports_calibration.py

      # === Multi-sport top picks aggregator (cross-sport "best of everything") ===
      - name: Multi-sport top picks aggregator
        working-directory: python
        run: python multi_sport_top.py
      - name: Multi-sport POT/POD history rollup
        working-directory: python
        run: python multi_sport_pot_rollup.py

      - name: NFL stub artifact
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python nfl_stub.py

      # Discord/Slack webhook digest (stubs when webhook env vars unset).
      - name: Push nightly digest to chat webhooks
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        working-directory: python
        run: python notify_discord.py

      # Per-market learning curves: rolling Brier per market
      - name: Compute learning curves
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python learning_curves.py

      # Context-conditional bias slices (indoor/outdoor, L/R pitcher)
      - name: Context-conditional calibration
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python context_calibration.py

      # Loop value quantifier (raw vs calibrated A/B)
      - name: Quantify loop value-add
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python loop_value.py

      # Per-market training progress + ETA
      - name: Training progress + ETA
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python training_progress.py

      # Recommendation trust score
      - name: Recommendation trust score
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python recommendation_trust.py

      # Trap-bet detector: post-best-bets pass that flags props with
      # structural red flags (weather against, SP fatigue against K OVER,
      # thin model sample, BvP pitcher-owns-batter, etc.).
      - name: Trap detector
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python trap_detector.py

      # Per-prop confidence interval via bootstrap (uncertainty band on the
      # model probability for every best bet).
      - name: Per-prop confidence intervals
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python per_prop_ci.py

      # Weather drift detector: alerts when game-time wind/temp shifts
      # materially vs morning forecast.
      - name: Weather drift detector
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python forecast_weather_drift.py

      # Per-player rolling-5-game streak chart data for /player sparklines.
      - name: Build streak chart data
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python streak_chart_data.py

      # "Anyone HR in this game" probability + fair odds per game.
      - name: Score Anyone-HR fair odds
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python anyone_hr.py

      # Auto-brief: tight markdown digest of tonight's slate.
      # Pulls from best_bets, slate_confidence, fatigue, weather, hot_streaks,
      # nrfi, sgps, bullpen, traps, portfolio, bankroll_sim. Powers /brief.
      - name: Generate auto-brief
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python auto_brief.py

      # POD history: snapshot today's Play of the Day into rolling record +
      # re-settle prior PODs against accumulated outcomes. Powers /pod-history.
      # MUST run AFTER pipeline.py (which writes play_of_day to today.json)
      # and outcomes.py (which writes track_record settlement results).
      - name: Snapshot + settle Play-of-Day history
        working-directory: python
        run: python pod_history.py

      # Golf: pull active PGA tournament leaderboard from ESPN + run
      # Monte Carlo win / top-5 / top-10 / top-20 simulation.
      # Both run every cron so we capture data even during weekday-only
      # opening rounds. Free ESPN API, no quota concerns.
      - name: Pull PGA Tour leaderboard (ESPN)
        working-directory: python
        run: python golf_pipeline.py
      - name: Monte Carlo golf winner probabilities
        working-directory: python
        run: python golf_winner_model.py
      - name: Compute golf head-to-head matchup probabilities
        working-directory: python
        run: python golf_h2h.py
      - name: Compute golf make-cut + top-nationality props
        working-directory: python
        run: python golf_makecut.py
      - name: Select golf Play of the Tournament
        working-directory: python
        run: python golf_bestbet.py
      - name: Snapshot + settle Golf POT history
        working-directory: python
        run: python golf_pot_history.py
      - name: Build golf 2-3-leg parlays
        working-directory: python
        run: python golf_parlay.py

      # Comprehensive pipeline audit -- LAST step so it sees final artifact
      # state. Writes data/pipeline_audit.json with overall: green/yellow/red,
      # per-artifact freshness, and findings. Surfaces on /audit.
      - name: Run pipeline audit (last)
        if: always()
        working-directory: python
        run: python pipeline_audit.py

      # Failure notifier: if any prior step failed, post a Discord ping.
      # if: failure() catches earlier failures so this fires only when needed.
      - name: Notify on pipeline failure
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            curl -s -X POST -H "Content-Type: application/json" \
              -d "{\"content\": \"⚠️ **EdgeStat daily pipeline FAILED** at $(date -u +%FT%TZ). Check Actions log: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\"}" \
              "$DISCORD_WEBHOOK_URL"
          fi

      # Pull PrizePicks daily board, compare to DK, run model at PP's lines.
      - name: Run pick-em (PrizePicks) integration
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python pickem.py

      # PrizePicks Power-Play optimizer: build optimal 2-6 leg combinations.
      - name: Optimize PrizePicks Power Plays
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python pickem_optimizer.py

      # Sleeper Picks integration. Stubs out gracefully when SLEEPER_TOKEN is unset.
      - name: Run Sleeper Picks integration (auth-gated)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        env:
          SLEEPER_TOKEN: ${{ secrets.SLEEPER_TOKEN }}
        working-directory: python
        run: python sleeper.py

      # Daily injury tracker: 40-man roster IL pulls per team.
      - name: Pull IL status (all 30 teams)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python injury_tracker.py

      # Last-10 team form for hot/cold signal overlay.
      - name: Pull team form (last 10 per team)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python team_form.py

      # Bullpen workload: flags teams with gassed relievers (> 8 IP in 2 days).
      # Useful signal for totals (gassed pen -> more late-inning runs likely).
      - name: Pull bullpen workload (last 2 days)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python bullpen.py

      # NRFI / YRFI projections (No Runs First Inning).
      - name: Project NRFI / YRFI per game
        working-directory: python
        run: python nrfi.py

      # First-5 (F5) totals -- isolates starting pitcher edge from bullpen.
      - name: Project First-5 totals per game
        working-directory: python
        run: python first_five.py

      # Lineup quality aggregator: sum each confirmed batter's xwOBA x PA.
      - name: Score confirmed lineup quality
        working-directory: python
        run: python lineup_quality.py

      # Run-line (-1.5 / +1.5) projections via Skellam normal approximation.
      - name: Project run lines
        working-directory: python
        run: python run_line.py

      # Travel + rest day signal per team.
      - name: Compute travel + rest day signal
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python travel.py

      # Pitcher arsenal vs opposing lineup handedness leverage score.
      - name: Score pitcher arsenal vs lineup
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python pitch_matchup.py

      # Series momentum / context (Game 2 of series, blowout reactions, etc).
      - name: Compute series context
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python series_context.py

      # Pythagorean expectation (luck-adjusted record per team).
      - name: Pull Pythagorean expectations
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python pythag.py

      # MLB transactions feed (trades, call-ups, DFAs, IL moves -- last 5 days).
      - name: Pull MLB transactions feed
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python transactions.py

      # Batter recent form (last 14 days hitting per starter in confirmed lineups).
      - name: Pull batter recent form
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python batter_form.py

      # Pitcher days of rest (catches short rest / extra rest situations).
      - name: Compute pitcher days of rest
        working-directory: python
        run: python pitcher_rest.py

      # Research depth score per game (how complete our data stack is).
      - name: Score research depth per game
        working-directory: python
        run: python research_depth.py

      # Best batter-vs-pitcher xwOBA matchups (leverage / fade spots).
      - name: Score best matchups (batter-vs-pitch xwOBA)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python best_matchups.py

      # Model trainer: walk-forward optimization once enough outcomes accumulate.
      - name: Train + tune model parameters
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        working-directory: python
        run: python model_trainer.py

      # CLV computation: compare today's morning snapshot to the latest (near
      # closing) lines. Best run at 6 PM ET, but no harm running every cron.
      - name: Compute Closing Line Value
        working-directory: python
        run: python clv.py

      # PROP CLV: same open->close lifecycle as clv.py, but for player props --
      # the edge the track record actually rests on. 6 AM freezes the open,
      # 6 PM captures the close; prop_clv.json holds the rollup.
      - name: Compute PROP Closing Line Value
        working-directory: python
        run: python prop_clv.py

      # Attribute prop CLV to each source via the canonical taxonomy -- does each
      # curated feed beat the close on its props? (Fills in as slates accrue.)
      - name: Prop CLV by source (curated-edge confirmation)
        working-directory: python
        run: python prop_clv_by_source.py

      # Aggregate today's top plays across DraftKings, PrizePicks, and parlays.
      - name: Aggregate Top Plays across all markets
        working-directory: python
        run: python top_plays.py

      # Generate the daily markdown brief from all of today's artifacts.
      - name: Generate daily summary brief
        working-directory: python
        run: python daily_summary.py

      # Health audit: writes data/pipeline_health.json with per-artifact
      # status (ok/empty/stale/demo). Surfaces on /audit.html.
      - name: Pipeline health audit
        working-directory: python
        run: python pipeline_health.py

      # Push to Discord / Slack if webhook URLs are configured as repo secrets.
      - name: Notify channels (Discord / Slack)
        if: github.event.schedule == '0 10 * * *' || github.event_name == 'workflow_dispatch'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        working-directory: python
        run: python notify.py

      - name: Commit + push if data changed
        # RESILIENCE (added 2026-06-02): run even when an earlier step failed, so a
        # single bad module can never again freeze the slate. On 2026-06-01 a
        # NameError in props_pipeline.py aborted the job BEFORE this step, so a
        # freshly-computed today.json was never published and the site served
        # ~17h-stale data. With always(), whatever fresh data WAS produced still
        # ships; the job still reports failure (so the if:failure alert + the
        # auto-retrigger watchdog still fire) -- we get the signal AND the data.
        if: always()
        env:
          # Headless: never open EDITOR on rebase-continue / commit-edit
          GIT_EDITOR: "true"
          GIT_SEQUENCE_EDITOR: "true"
        run: |
          git config user.name  "edgestat-bot"
          git config user.email "actions@github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No data changes — nothing to commit."
          else
            git commit -m "chore(data): daily refresh $(date -u +%FT%TZ)"
            # Robust retry: if rebase hits conflicts on data/*.json, just take
            # OUR version (we just produced fresh data, ours is newer than remote)
            for i in 1 2 3 4 5; do
              git fetch origin main
              # If our HEAD already includes origin/main, push directly
              if git merge-base --is-ancestor origin/main HEAD; then
                git push && { echo "pushed cleanly"; exit 0; }
                sleep 3
                continue
              fi
              # Attempt rebase; on conflict take our version for any data/*.json
              if git rebase origin/main; then
                git push && { echo "pushed after rebase"; exit 0; }
              else
                # Conflict path: take ours for everything, then continue with
                # GIT_EDITOR=true so the rebase-continue commit message edit
                # doesn't try to open an interactive editor.
                git checkout --ours . 2>/dev/null || true
                git add -A
                GIT_EDITOR=true git rebase --continue || git rebase --abort
                git push && { echo "pushed after conflict resolution"; exit 0; }
              fi
              sleep 5
            done
            echo "push failed after 5 retries"
            exit 1
          fi
